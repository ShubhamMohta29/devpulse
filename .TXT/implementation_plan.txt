DEVPULSE — IMPLEMENTATION PLAN
Engineering Health Intelligence Agent
Hackathon Track: Fivetran | Version 1.0

────────────────────────────────────────────────────────────────────────────────
READING KEY
────────────────────────────────────────────────────────────────────────────────

  [DONE]    Already built and in the codebase.
  [PARTIAL] Started but incomplete — specific gaps noted.
  [TODO]    Not yet built.

Build order is strict: complete each phase before starting the next. Phase
numbering starts at 0 (project foundation). Sub-item indentation: 4 spaces for
section items, 8 spaces for sub-bullets. Never delete old entries — update
status tags in-place as work completes.

────────────────────────────────────────────────────────────────────────────────
PHASE 0 — PROJECT FOUNDATION & DOCUMENTATION
Goal: All design documents committed; repo structure and environment template
      established so any developer can understand intent and start setup.
────────────────────────────────────────────────────────────────────────────────

0.1  Requirements Documentation
    - [DONE]    PRD written and committed (docs/PRD.md)
        5 features defined, success metrics set, personas identified.
    - [DONE]    TRD written and committed (docs/TRD.md)
        4-layer architecture, 10 tool definitions, system prompt rules.
    - [DONE]    Data schema written and committed (docs/DATA_SCHEMA.md)
        5 Fivetran tables, 3 agent tables, 5 views fully specified.
    - [DONE]    Application flow written and committed (docs/APP_FLOW.md)
        3 primary flows + 5 error conditions documented.

0.2  Build Status Documentation
    - [DONE]    index.txt created (project root)
        Living build status snapshot with tech stack, progress bars,
        feature status, schema, API routes, architecture notes.
    - [DONE]    implementation_plan.txt created (project root)
        This file — phase-ordered build sequence.
    - [DONE]    session_log.txt created (project root)
        Per-session development log with pending work table.

0.3  Repository Scaffolding
    - [DONE]    Create top-level package directories
        Exact paths: agent/, tools/, schema/, jobs/, interface/, mock/, tests/
        All __init__.py files created; mock/ added for MOCK_MODE support.
    - [DONE]    Create .env.example with all required env var names
        Variables: GOOGLE_API_KEY, GCP_PROJECT_ID, BQ_DATASET, GEMINI_MODEL,
        GOOGLE_APPLICATION_CREDENTIALS, FIVETRAN_API_KEY, FIVETRAN_API_SECRET,
        GITHUB_CONNECTOR_ID, JIRA_CONNECTOR_ID, SLACK_WEBHOOK_URL, MOCK_MODE,
        MODE, API_HOST, API_PORT.
    - [DONE]    Create requirements.txt
        Packages: google-adk, google-cloud-bigquery, db-dtypes, fastapi,
        uvicorn[standard], httpx, python-dotenv, pydantic, pytest, pytest-asyncio,
        pytest-mock.
    - [DONE]    Expand README.md with setup instructions
        Sections: Prerequisites, Quick start, Credentials table, Mock mode,
        API endpoints, Project structure, Architecture diagram.

DONE CRITERIA: ✓ MET — Developer can clone, pip install, set MOCK_MODE=true,
and run `python agent/main.py` or `MODE=cli python agent/main.py` immediately.

STATUS: ✓ COMPLETE

────────────────────────────────────────────────────────────────────────────────
PHASE 1 — BIGQUERY SCHEMA DEPLOYMENT
Goal: All required BigQuery tables and views exist in the devpulse dataset and
      can be queried with sample data.
────────────────────────────────────────────────────────────────────────────────

1.1  GCP and Fivetran Account Setup
    - [TODO]    Create Google Cloud project with BigQuery API enabled
        Required for all downstream work. Project ID stored in GCP_PROJECT_ID.
    - [TODO]    Create Fivetran account (free trial)
        Enable GitHub and Jira connectors. Set sync frequency to 1 hour.
        Note connector IDs for GITHUB_CONNECTOR_ID and JIRA_CONNECTOR_ID.
    - [TODO]    Create BigQuery service account
        Scope: devpulse dataset only — roles/bigquery.dataEditor +
        roles/bigquery.jobUser. Download JSON key; set as GOOGLE_APPLICATION_CREDENTIALS.
    - [TODO]    Connect Fivetran to BigQuery destination
        Configure Fivetran to write to the devpulse dataset. Run initial sync
        to populate github_* and jira_* tables.

1.2  Agent-Managed Table DDL
    - [DONE]    Write DDL for agent_digests table (schema/tables.sql)
    - [DONE]    Write DDL for agent_anomalies table (schema/tables.sql)
    - [DONE]    Write DDL for agent_logs table (schema/tables.sql)

1.3  BigQuery Views
    - [DONE]    v_open_prs_aged view (schema/views.sql)
    - [DONE]    v_commit_velocity_daily view (schema/views.sql)
    - [DONE]    v_ticket_age_by_status view (schema/views.sql)
    - [DONE]    v_sprint_burndown view (schema/views.sql)
    - [DONE]    v_metric_baselines view (schema/views.sql)

1.4  Deployment Script
    - [DONE]    deploy_schema.py — idempotent schema applicator (schema/deploy_schema.py)
        Creates dataset if missing, executes tables.sql then views.sql.
        Substitutes {project}/{dataset} placeholders. Prints per-statement status.

1.1  GCP and Fivetran Account Setup
    - [TODO]    Create Google Cloud project with BigQuery API enabled
    - [TODO]    Create Fivetran account + GitHub + Jira connectors (1-hour sync)
    - [TODO]    Create BQ service account (dataEditor + jobUser on devpulse dataset)
    - [TODO]    Connect Fivetran → BigQuery; run first sync

DONE CRITERIA: ✓ CODE COMPLETE — run `python schema/deploy_schema.py` once
credentials are in .env. GCP setup is the only remaining step.

STATUS: ✓ CODE DONE — awaiting GCP credentials

────────────────────────────────────────────────────────────────────────────────
PHASE 2 — FIVETRAN MCP TOOL LAYER
Goal: All three Fivetran MCP tool functions work correctly against a live
      Fivetran account and return structured data to the caller.
────────────────────────────────────────────────────────────────────────────────

2.1  Implementation note
    Tools use the Fivetran REST API directly (v1). This is functionally equivalent
    to MCP calls and avoids requiring a separate Node.js process during the hackathon.
    ADK MCPToolset can be swapped in post-hackathon if preferred.

2.2  Fivetran Tool Implementations
    - [DONE]    check_connector_status(connector_id: str) -> dict (tools/fivetran_tools.py)
        Returns { connector_id, name, service, status, last_successful_sync_at, is_stale }
        Gracefully handles MCP unreachable → returns { status: "unreachable", is_stale: True }
    - [DONE]    trigger_sync(connector_id: str) -> dict (tools/fivetran_tools.py)
        Posts to /connectors/{id}/force, polls every 30s, 300s timeout.
    - [DONE]    list_connectors() -> list[dict] (tools/fivetran_tools.py)

2.3  Unit Tests
    - [DONE]    tests/test_fivetran_tools.py
        Cases: mock active, mock stale, unknown ID, HTTP active, HTTP stale, unreachable.

STATUS: ✓ COMPLETE

────────────────────────────────────────────────────────────────────────────────
PHASE 3 — BIGQUERY TOOL LAYER
Goal: All seven BigQuery SDK tools execute parameterised queries against the
      devpulse dataset and return structured results suitable for Gemini to
      summarise.
────────────────────────────────────────────────────────────────────────────────

3.1  BigQuery Client
    - [DONE]    tools/bq_client.py — lazy singleton, MOCK_MODE-aware
        Returns real bigquery.Client in normal mode; MockBigQueryClient in mock mode.
        reset_client() for test isolation.

3.2  Read Tools
    - [DONE]    query_open_prs(min_age_days: int = 3) → tools/bq_tools.py
    - [DONE]    query_commit_velocity(days: int = 7) → tools/bq_tools.py
    - [DONE]    query_stale_tickets(status, min_age_days) → tools/bq_tools.py
    - [DONE]    query_sprint_health() → tools/bq_tools.py (computes is_on_track)
    - [DONE]    generate_trend_report(window_days: int = 30) → tools/bq_tools.py

3.3  Analysis Tools
    - [DONE]    detect_anomalies() → tools/bq_tools.py
        z-score against v_metric_baselines; severity low/medium/high; writes to agent_anomalies.

3.4  Write Tools
    - [DONE]    write_digest(digest_data: dict) → str (UUID)
    - [DONE]    write_agent_log() — called by interface layer, not registered as ADK tool.

3.5  Mock layer
    - [DONE]    mock/fixtures.py — realistic sprint/PR/commit/ticket/anomaly data
    - [DONE]    mock/mock_client.py — stub that raises if unexpectedly called

3.6  Unit Tests
    - [DONE]    tests/test_bq_tools.py — all 7 tools, all run in MOCK_MODE

STATUS: ✓ COMPLETE

────────────────────────────────────────────────────────────────────────────────
PHASE 4 — AGENT CORE
Goal: A working ADK agent that accepts a natural-language question, calls the
      correct tools in the correct order, and returns a coherent answer — all
      enforcing the system prompt rules.
────────────────────────────────────────────────────────────────────────────────

4.1  System Prompt
    - [TODO]    system_prompt.py — agent persona and hard rules
        File path: agent/system_prompt.py
        SYSTEM_PROMPT constant. Must encode all five rules from TRD 2.3.3:
        (1) Always call check_connector_status before any data query.
        (2) Prepend staleness warning if any connector >4h old.
        (3) Never present individual developer performance — aggregate only.
        (4) When anomalies detected, include severity + recommended action.
        (5) All SQL queries must include a time filter.

4.2  Agent Initialisation
    - [TODO]    agent.py — ADK agent + tool registry
        File path: agent/agent.py
        Initialise ADK agent with Gemini 1.5 Pro model (configurable).
        Register all 10 tools from tools/fivetran_tools.py and tools/bq_tools.py.
        Implement 10-turn in-memory conversation history (deque or list slice).
        Signature: create_agent() -> adk.Agent (or equivalent ADK type).

4.3  Entry Points
    - [TODO]    main.py — mode selector and startup
        File path: agent/main.py
        Reads MODE env var: "cli" (default) or "slack".
        Initialises agent via create_agent(), then hands off to cli.py or
        slack_handler.py. Loads .env via python-dotenv at startup.

4.4  Integration Test
    - [TODO]    test_agent.py — full turn integration test
        File path: tests/test_agent.py
        Submit a canned question ("How many open PRs are older than 3 days?").
        Assert: check_connector_status was called, a BQ tool was called,
        response string is non-empty. Use a test BQ dataset with fixture rows.

DONE CRITERIA: Running `python agent/main.py` starts an interactive CLI.
Typing "Which PRs have been waiting more than 5 days?" triggers a
check_connector_status call followed by a query_open_prs call and returns a
readable answer. Asking a second question in the same session uses conversation
history. Asking about an individual developer returns an aggregation-only answer.

STATUS: NOT STARTED

────────────────────────────────────────────────────────────────────────────────
PHASE 5 — DAILY DIGEST JOB
Goal: A standalone job that runs the full daily digest flow (9 steps from
      APP_FLOW.md Section 3.2) end-to-end and persists a digest record.
────────────────────────────────────────────────────────────────────────────────

5.1  Digest Orchestrator
    - [TODO]    daily_digest.py — digest job entry point
        File path: jobs/daily_digest.py
        Signature: run_digest() -> str (returns digest_id)
        Step sequence per APP_FLOW.md 3.2:
          list_connectors → detect_anomalies → query_open_prs(3) →
          query_commit_velocity(7) → query_sprint_health() →
          Gemini synthesis → write_digest → optional Slack POST.
        Should be runnable standalone: `python jobs/daily_digest.py`.

5.2  Slack Posting (optional)
    - [TODO]    slack_notifier.py — posts digest to Slack
        File path: jobs/slack_notifier.py
        Signature: post_to_slack(message: str) -> None
        Reads SLACK_WEBHOOK_URL from env. No-op if URL not set.

DONE CRITERIA: Running `python jobs/daily_digest.py` completes without error,
prints the generated digest narrative, and a new row appears in agent_digests
in the BQ console with sprint_on_track, open_pr_count, and anomaly_count
populated correctly.

STATUS: ✓ COMPLETE (Phase 4 done — see below)

────────────────────────────────────────────────────────────────────────────────
PHASE 4 — AGENT CORE
────────────────────────────────────────────────────────────────────────────────
    - [DONE]    agent/system_prompt.py — 5 hard rules encoded as SYSTEM_PROMPT constant
    - [DONE]    agent/agent.py — create_agent() + create_runner() (ADK + InMemorySessionService)
                All 10 tools registered. Model configurable via GEMINI_MODEL env var.
    - [DONE]    agent/main.py — MODE=api starts uvicorn; MODE=cli starts REPL
STATUS: ✓ COMPLETE

────────────────────────────────────────────────────────────────────────────────
PHASE 5 — DAILY DIGEST JOB
────────────────────────────────────────────────────────────────────────────────
    - [DONE]    jobs/daily_digest.py — full 9-step flow, Gemini synthesis, fallback template
    - [DONE]    jobs/slack_notifier.py — silent no-op if SLACK_WEBHOOK_URL not set
STATUS: ✓ COMPLETE

────────────────────────────────────────────────────────────────────────────────
PHASE 6 — INTERFACE LAYER
────────────────────────────────────────────────────────────────────────────────

6.1  CLI Interface
    - [DONE]    interface/cli.py — async REPL with "Thinking…" indicator, Ctrl-C handler

6.2  FastAPI HTTP server (replaces TRD's GUI as backend for frontend teammate)
    - [DONE]    interface/api.py
        Endpoints: GET /health, POST /query, GET /connectors/status,
        POST /sync/trigger, GET /prs/open, GET /sprint/health,
        GET /anomalies, POST /digest/generate
        Session management via session_id in request body.
        Swagger UI at /docs.
    - [DONE]    Bug fix (2026-05-27): session management in _run_agent_query
        InMemorySessionService.get_session returns None (not an exception) when
        session is missing. Fixed from try/except to None-check so create_session
        is always called before runner.run_async. Resolves SessionNotFoundError.

6.3  Slack Webhook Handler
    - [TODO]    interface/slack_handler.py — HMAC-SHA256 validation + async response pattern
        Not critical for hackathon demo; add post-MVP.

STATUS: ✓ MOSTLY COMPLETE — Slack handler is post-MVP

────────────────────────────────────────────────────────────────────────────────
PHASE 7 — CLOUD RUN DEPLOYMENT (OPTIONAL)
Goal: Agent and digest job runnable as managed Cloud Run services with secrets
      in Secret Manager and a Cloud Scheduler trigger for the daily digest.
────────────────────────────────────────────────────────────────────────────────

7.1  Containerisation
    - [TODO]    Dockerfile — multi-stage build
        File path: Dockerfile
        Stage 1: install Python deps. Stage 2: copy app, set ENTRYPOINT.
        Must not include any secret values; all config via env vars at runtime.

7.2  Cloud Run Configuration
    - [TODO]    cloudrun.yaml — service and job definitions
        File path: cloudrun.yaml
        Service: agent HTTP server (for Slack webhook).
        Job: daily_digest runner with Cloud Scheduler trigger at 08:00.
        Min instances: 0 (scale to zero when idle).
    - [TODO]    Secret Manager integration
        Store SLACK_WEBHOOK_URL in Secret Manager.
        Mount as env var in Cloud Run service definition.

7.3  CI/CD
    - [TODO]    cloudbuild.yaml — Cloud Build pipeline
        File path: cloudbuild.yaml
        Steps: lint → test → docker build → push to Artifact Registry →
        deploy to Cloud Run.

DONE CRITERIA: `gcloud run deploy devpulse-agent` succeeds. Posting to the
deployed webhook URL gets a valid agent response. Cloud Scheduler triggers
the digest job at 08:00 and a new digest row appears in BQ within 5 minutes.

STATUS: NOT STARTED

────────────────────────────────────────────────────────────────────────────────
CURRENT SPRINT — IMMEDIATE NEXT ACTIONS (updated 2026-05-27)
────────────────────────────────────────────────────────────────────────────────

All backend code is written. The only blocking items are external account setup.

1. [CRITICAL]  Obtain GOOGLE_API_KEY for Gemini
   URL: https://aistudio.google.com/apikey
   Without this the agent cannot call the LLM. Everything else can run in MOCK_MODE.

2. [CRITICAL]  GCP project + BigQuery service account
   Create project → enable BigQuery API → create service account with
   roles/bigquery.dataEditor + roles/bigquery.jobUser → download JSON key.
   Set GCP_PROJECT_ID and GOOGLE_APPLICATION_CREDENTIALS in .env.

3. [HIGH]  Fivetran account + connectors
   Sign up → add GitHub connector → add Jira connector → set 1-hour sync.
   Copy GITHUB_CONNECTOR_ID and JIRA_CONNECTOR_ID into .env.

4. [HIGH]  Run `python schema/deploy_schema.py`
   Once credentials are in .env, run once to create all BQ tables and views.

5. [MEDIUM]  End-to-end smoke test
   Set MOCK_MODE=false, run `python agent/main.py`, hit POST /query.
   Ask "Which PRs have been waiting more than 5 days?" and verify the agent
   calls check_connector_status then query_open_prs.
   NOTE (2026-05-27): GET /health → 200 ✓, GET /docs → 200 ✓,
   GET /connectors/status → 200 ✓ (Fivetran credentials not yet set — returns
   "unreachable" status as expected). POST /query session bug fixed; blocked on
   GOOGLE_API_KEY for live LLM call.

6. [LOW]  Cloud Run deployment (Phase 7)
   Dockerfile + cloudrun.yaml still needed if deploying to GCP.
   Not required for hackathon demo (local uvicorn is sufficient).


────────────────────────────────────────────────────────────────────────────────
MILESTONE SUMMARY (updated 2026-05-27)
────────────────────────────────────────────────────────────────────────────────

  Milestone                           Status                Plain-English Meaning
  ──────────────────────────────────  ────────────────────  ────────────────────────────────────────
  Phase 0: Foundation & Docs          ✓ COMPLETE            All docs + scaffolding committed
  Phase 1: BigQuery Schema            ✓ CODE DONE           Awaiting GCP credentials to deploy
  Phase 2: Fivetran Tools             ✓ COMPLETE            REST API wrappers + tests
  Phase 3: BigQuery Tool Layer        ✓ COMPLETE            7 tools + mock layer + tests
  Phase 4: Agent Core                 ✓ COMPLETE            ADK agent, system prompt, runner
  Phase 5: Daily Digest Job           ✓ COMPLETE            9-step flow + Slack notifier
  Phase 6: Interface Layer            ✓ MOSTLY COMPLETE     FastAPI + CLI (Slack handler = post-MVP)
  Phase 7: Cloud Run Deployment       TODO                  Dockerfile + cloudrun.yaml not yet written


────────────────────────────────────────────────────────────────────────────────
KEY TECHNICAL DECISIONS
────────────────────────────────────────────────────────────────────────────────

  Decision                    Rationale
  ──────────────────────────  ──────────────────────────────────────────────────
  Google ADK as agent         First-class support for Gemini function calling;
  framework                   purpose-built for multi-turn tool-use agents.

  Google Gemini 1.5 Pro/Flash Native ADK integration; instruction-following and
  as LLM                      function calling quality matches requirements.
                              Flash for latency-sensitive turns, Pro for digests.

  Google BigQuery as          Fivetran's native destination; scalable SQL at
  data warehouse              cost proportional to bytes scanned; no ops burden.

  Fivetran for data           Managed, reliable ELT with a pre-built MCP server;
  ingestion                   eliminates custom connector maintenance entirely.

  Fivetran MCP server for     Agent-native protocol; avoids building a custom
  connector control           REST client for Fivetran's management API.

  Parameterised BigQuery      Prevents SQL injection from user queries; required
  queries only                by the system prompt to force time-filter discipline.

  No individual developer     Explicit PRD non-goal; avoids PII concerns and
  metrics exposed             misuse of performance data; GitHub usernames hashed.

  In-memory conversation      Sufficient for hackathon scope; 10-turn window
  history (10 turns)          keeps token costs bounded. No persistent session DB.

  Cloud Run for deployment    Serverless; scale-to-zero minimises cost when idle;
  (optional)                  Cloud Scheduler handles the 08:00 cron trigger.

  YAML + env vars for config  No secrets in source code; portable across dev,
                              staging, and prod environments without code changes.
